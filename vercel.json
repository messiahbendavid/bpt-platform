{
  "version": 2,
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @bpt/shared build && pnpm --filter @bpt/web build",
  "outputDirectory": "apps/web/dist",
  "framework": null
}
